services:
  backend:
    build: ./backend #path of dockerfile
    ports:
      - 5000:5000

  frontend:
    build: ./frontend
    ports:
      - 5137:5137

  redis:
    image: redis
    ports:
      - 6379:6379
